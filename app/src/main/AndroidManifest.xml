<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.didi.myapplication">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/> <!--获取当前wifi状态 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ImageScaleActivity">
            <intent-filter>
                <action android:name="com.didi.virtualapk.Activity1" />
                <action android:name="@string/hehe" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="@string/app_scheme" />
            </intent-filter>
        </activity>
        <activity android:name=".AActivity" />
        <activity android:name=".MapUriSchemeTestActivity" />
        <activity android:name=".BActivity"
            android:theme="@style/BtsTheme.NoTitle"/>
        <activity android:name=".CActivity" />
    </application>

</manifest>